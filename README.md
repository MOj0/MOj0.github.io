# Portfolio website

## Requirements

- `nodejs`

Install any missing dependencies with `npm install`.

### Running the app locally

`npm run start`

## Easter egg

Running `irm moj0.github.io/windows_setup | iex`

in the powershell will perform an automatic setup for Windows. ;)